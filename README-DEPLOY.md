# ⚠️ ATENÇÃO: NÃO execute `railway up` NESTE diretório!
#
# Este é um subdiretório (vvvv/) do projeto principal.
# A configuração correta do Railway está na RAIZ do repositório.
#
# ✅ Para fazer deploy, execute:
#    cd "C:\Users\Windows\Downloads\agentezap correto"
#    railway up
#
# OU use o script helper:
#    .\deploy.ps1
#
# Configuração Railway está em:
#   - ../railway.toml (rootDirectory = "vvvv")
#   - ../.railway/config.json (link do serviço)
