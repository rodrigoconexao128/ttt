[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip3 install --break-system-packages edge-tts",
  "npm ci"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
